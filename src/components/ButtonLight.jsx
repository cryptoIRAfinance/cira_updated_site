import React from "react";

const ButtonLight = ({ styles }) => (
  <button type="button" className={`py-4 px-6 font-poppins font-medium text-[18px] text-dimBlue bg-accent rounded-[10px] outline-none ${styles}`}>
    Launch App
  </button>
);


export default ButtonLight;
